<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('link.edit', model: Book.model_name.human), edit_book_path(@book) %> |
  <%= link_to t('link.back', model: Book.model_name.human), books_path %>

  <%= button_to t('link.destroy', model: Book.model_name.human), @book, method: :delete %>
</nav>
